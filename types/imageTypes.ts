export interface ImageCfg {
    blob: string;
    artists: Array<string>;
    characters: Array<string>;
    meta: Array<string>;
}

export interface ImageCard {
    link: string;
    artists: Array<string>;
    characters: Array<string>;
    meta: Array<string>;
}